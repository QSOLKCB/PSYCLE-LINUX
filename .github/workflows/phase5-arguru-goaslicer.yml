name: Phase 5 Arguru Goaslicer preservation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  arguru-goaslicer:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Arguru Goaslicer preservation regression
        run: |
          chmod +x scripts/phase5-arguru-goaslicer-smoke.sh
          scripts/phase5-arguru-goaslicer-smoke.sh phase5-arguru-goaslicer

      - name: Upload Arguru Goaslicer evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase5-arguru-goaslicer
          path: phase5-arguru-goaslicer/
          if-no-files-found: warn
          retention-days: 30
