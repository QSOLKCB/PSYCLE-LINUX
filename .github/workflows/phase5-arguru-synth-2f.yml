name: Phase 5 Arguru Synth 2f preservation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  arguru-synth-2f:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Arguru Synth 2f preservation regression
        run: |
          chmod +x scripts/phase5-arguru-synth-2f-smoke.sh
          scripts/phase5-arguru-synth-2f-smoke.sh phase5-arguru-synth-2f

      - name: Upload Arguru Synth 2f evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase5-arguru-synth-2f
          path: phase5-arguru-synth-2f/
          if-no-files-found: warn
          retention-days: 30
