name: Phase 5 Audacity Compressor preservation

on:
  push:
    branches:
      - main
      - 'phase-5/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audacity-compressor:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Audacity Compressor preservation regression
        shell: bash
        run: |
          set -o pipefail
          chmod +x scripts/phase5-audacity-compressor-smoke.sh
          scripts/phase5-audacity-compressor-smoke.sh phase5-audacity-compressor \
            2>&1 | tee "$RUNNER_TEMP/phase5-audacity-compressor.log"

      - name: Upload Audacity Compressor evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: phase5-audacity-compressor
          path: |
            phase5-audacity-compressor/
            ${{ runner.temp }}/phase5-audacity-compressor.log
          if-no-files-found: warn
