name: Phase 5 Audacity Phaser preservation

on:
  push:
    branches:
      - main
      - 'phase-5/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audacity-phaser:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Audacity Phaser preservation regression
        shell: bash
        run: |
          set -o pipefail
          chmod +x scripts/phase5-audacity-phaser-smoke.sh
          scripts/phase5-audacity-phaser-smoke.sh phase5-audacity-phaser \
            2>&1 | tee "$RUNNER_TEMP/phase5-audacity-phaser.log"

      - name: Upload Audacity Phaser evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: phase5-audacity-phaser
          path: |
            phase5-audacity-phaser/
            ${{ runner.temp }}/phase5-audacity-phaser.log
          if-no-files-found: warn
