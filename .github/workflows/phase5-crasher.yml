name: Phase 5 Crasher preservation

on:
  push:
    branches:
      - main
      - 'phase-5/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  crasher:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Crasher preservation regression
        shell: bash
        run: |
          set -o pipefail
          chmod +x scripts/phase5-crasher-smoke.sh
          scripts/phase5-crasher-smoke.sh phase5-crasher \
            2>&1 | tee "$RUNNER_TEMP/phase5-crasher.log"

      - name: Upload Crasher evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: phase5-crasher
          path: |
            phase5-crasher/
            ${{ runner.temp }}/phase5-crasher.log
          if-no-files-found: warn
