name: Phase 5 Negative preservation

on:
  push:
    branches:
      - main
      - 'phase-5/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  negative:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install native-machine test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            pkg-config \
            liblilv-dev \
            liblua5.4-dev

      - name: Run Negative preservation regression
        shell: bash
        run: |
          set -o pipefail
          chmod +x scripts/phase5-negative-smoke.sh
          scripts/phase5-negative-smoke.sh phase5-negative \
            2>&1 | tee "$RUNNER_TEMP/phase5-negative.log"

      - name: Upload Negative evidence
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: phase5-negative
          path: |
            phase5-negative/
            ${{ runner.temp }}/phase5-negative.log
          if-no-files-found: warn
